import styled from 'styled-components';

const Svg = styled.svg`
  display: inline-block;
  width: 1em;
  height: 1em;
  font-size: 16px;
  vertical-align: text-top;
  fill: currentColor;
  stroke: currentColor;
  pointer-events: none;
`;

export default {
  Svg,
};
